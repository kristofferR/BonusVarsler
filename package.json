{
  "name": "bonusvarsler",
  "version": "9.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.js",
    "build:src": "bun run scripts/bundle.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "playwright": "^1.59.1",
    "typescript": "^7.0.2"
  }
}
